module src/main 

import src/lib/runner/runPipeline


def main(): Unit = {
  
  runPipeline(
    """if (10 < 20) {
      if (true) {
        val x = 2
        x
      }
    } else {
      val x = 5
      x
    }
    """
  )
  ()

}