module src/test

import test
import src/lib/lexer

def main() = mainSuite("lib") {
  test("lexer produces tokens") {
    report {
      var tokens = lexer[Unit]("foo()") {
        assertEqual(do next().show, "identifier")
        assertEqual(do next().show, "punctuation")
        assertEqual(do next().show, "punctuation")
      }
    }
  }

  // test("Hello world") {
  //   assertEqual(helloWorld(), "Hello, world!")
  // }
}
