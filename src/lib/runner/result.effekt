module src/lib/runner/result

import map
import src/lib/interpreter/values

type PipelineResult[T] {
  Success(data: T)
  Failure(msg: String)
}

record EvalState(value: Value, env: Map[String, Address], store: Map[Address, Value])